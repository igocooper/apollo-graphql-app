# name
# createdAt
# [todoId]

type Resolution {
    _id: String!,
    name: String!
}

type Mutation {
    createResolution(name: String!): Resolution,
    editResolution(query: String!, name: String!): Resolution,
    removeResolution(name: String!): Resolution
}